/** @type {import('next').NextConfig} */
const nextConfig = {
  reactStrictMode: true,
  images: {
    domains: ["localhost", "classroom-forhadx.herokuapp.com"],
  },
};

module.exports = nextConfig;
